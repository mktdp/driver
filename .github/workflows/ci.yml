name: CI

on:
  push:
  pull_request:

jobs:
  checks:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
          - os: windows-latest
    env:
      CARGO_TERM_COLOR: always
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go/fingerprint/go.mod

      - name: Cache cargo (Linux)
        if: runner.os == 'Linux'
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: cargo-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            cargo-${{ runner.os }}-

      - name: Cache cargo (Windows)
        if: runner.os == 'Windows'
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.USERPROFILE }}\.cargo\registry
            ${{ env.USERPROFILE }}\.cargo\git
          key: cargo-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            cargo-${{ runner.os }}-

      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            cmake \
            pkg-config \
            libusb-1.0-0-dev

      - name: Cache vcpkg downloads/binaries (Windows)
        if: runner.os == 'Windows'
        uses: actions/cache@v4
        with:
          path: |
            C:\vcpkg\downloads
            C:\vcpkg-bincache
          key: vcpkg-${{ runner.os }}-libusb-x64-${{ hashFiles('.github/workflows/ci.yml') }}
          restore-keys: |
            vcpkg-${{ runner.os }}-libusb-x64-

      - name: Install libusb via vcpkg (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          if (-not (Test-Path "C:\vcpkg\vcpkg.exe")) {
            git clone --depth 1 https://github.com/microsoft/vcpkg C:\vcpkg
            cmd.exe /d /c "C:\vcpkg\bootstrap-vcpkg.bat"
          }

          "VCPKG_ROOT=C:\vcpkg" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "VCPKG_BINARY_SOURCES=clear;files,C:\vcpkg-bincache,readwrite" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          & "C:\vcpkg\vcpkg.exe" install libusb:x64-windows

      - name: Patch nbis-rs for Windows MSVC build (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          . .\scripts\windows_common.ps1
          $cargoExe = Resolve-Cargo
          Patch-NbisRsWindowsMsvc -CargoPath $cargoExe

      - name: Run non-hardware CI checks (Linux)
        if: runner.os == 'Linux'
        run: ./scripts/ci_check.sh

      - name: Run non-hardware CI checks (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $env:CARGO_TARGET_DIR = Join-Path $env:RUNNER_TEMP "mktdp-target"
          powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\ci_check.ps1
